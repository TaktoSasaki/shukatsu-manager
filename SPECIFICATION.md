# 就活管理 (Shukatsu Manager) 仕様書

**バージョン**: 1.2.2  
**最終更新**: 2026-04-10  
**対象プラットフォーム**: iOS / Android (React Native + Expo)

---

## 目次

1. [アプリ概要](#1-アプリ概要)
2. [技術スタック](#2-技術スタック)
3. [画面構成とナビゲーション](#3-画面構成とナビゲーション)
4. [データモデル](#4-データモデル)
5. [画面別詳細仕様](#5-画面別詳細仕様)
6. [ステータス管理](#6-ステータス管理)
7. [選考イベント管理](#7-選考イベント管理)
8. [音声録音・文字起こし](#8-音声録音文字起こし)
9. [QRコードスキャン](#9-qrコードスキャン)
10. [通知](#10-通知)
11. [カレンダー連携](#11-カレンダー連携)
12. [並び替え](#12-並び替え)
13. [バリデーション](#13-バリデーション)
14. [権限](#14-権限)
15. [制限事項](#15-制限事項)
16. [デザインシステム](#16-デザインシステム)
17. [ビルド・リリース](#17-ビルドリリース)
18. [セキュリティ](#18-セキュリティ)

---

## 1. アプリ概要

就職活動における企業情報・選考履歴・面接メモを一元管理するモバイルアプリケーション。

### 主要機能

- 企業情報のCRUD（登録・閲覧・編集・削除）
- 選考イベントのタイムライン管理
- 選考結果に基づくステータス自動計算
- 面接音声のバックグラウンド録音とローカルAI文字起こし
- QRコードスキャン（説明会アンケート等）
- 端末カレンダーへの面接予定登録
- 面接当日のプッシュ通知リマインダー
- ドラッグ&ドロップによる企業カードの並び替え
- カスタムステータスの作成
- アプリ内カレンダーで面接予定を一覧表示
- 画像からのQRコード読み取り

---

## 2. 技術スタック

| 項目 | 技術 | バージョン |
|---|---|---|
| フレームワーク | Expo | 54 |
| UIフレームワーク | React Native | 0.81 |
| UIライブラリ | React | 19 |
| ルーティング | expo-router | 6 |
| データベース | expo-sqlite (SQLite) | 16 |
| 通知 | expo-notifications | 0.32 |
| カレンダー | expo-calendar | 15 |
| カメラ（QR） | expo-camera | 17 |
| 録音 | react-native-audio-record | 0.2 |
| バックグラウンド録音 | react-native-background-actions | 4 |
| 文字起こし | whisper.rn | 0.5 |
| ドラッグ&ドロップ | react-native-draggable-flatlist | 4 |
| アニメーション | react-native-reanimated | 4.1 |
| 画像ピッカー | expo-image-picker | 17 |
| クリップボード | expo-clipboard | 8 |
| 触覚フィードバック | expo-haptics | 15 |
| 音声再生 | expo-av | 16 |
| ファイル共有 | expo-sharing | 14 |
| ファイルシステム | expo-file-system | 19 |
| ジェスチャー | react-native-gesture-handler | 2.28 |
| SafeArea | react-native-safe-area-context | 5.6 |
| 日付ピッカー | @react-native-community/datetimepicker | 8.4 |
| UUID生成 | react-native-uuid | 2 |
| 言語 | TypeScript | 5.9 |

### 重要な前提

- **Expo Go では動作しない**（ネイティブ依存: whisper.rn, react-native-audio-record, react-native-background-actions）
- `expo run:android` / `expo run:ios` または EAS Build が必須
- React Native New Architecture 有効 (`newArchEnabled: true`)

---

## 3. 画面構成とナビゲーション

### ナビゲーション構造

```
RootLayout (Stack)
├── (tabs) [headerShown: false]
│   ├── index        → 企業一覧タブ ("企業一覧" / 🏢)
│   ├── calendar     → カレンダータブ ("カレンダー" / 📅)
│   └── qr-scan      → QRスキャンタブ ("QRスキャン" / 📷)
├── add              → 企業追加画面 (Modal)
└── [id]             → 企業詳細画面 (Push)
```

### ルートレイアウト

- **ヘッダー**: 背景色 `#4F46E5`（インディゴ）、白文字、太字
- **コンテンツ背景**: `#F9FAFB`
- **ステータスバー**: `light`（白文字）
- **GestureHandlerRootView + SafeAreaProvider** でラップ

### タブレイアウト

| タブ | タブラベル | ヘッダータイトル | アイコン |
|---|---|---|---|
| index | 企業一覧 | 就活管理 | 🏢 |
| calendar | カレンダー | カレンダー | 📅 |
| qr-scan | QRスキャン | QRスキャン | 📷 |

- アクティブタブ色: `#4F46E5`
- 非アクティブタブ色: `#9CA3AF`

---

## 4. データモデル

### 4.1 companies テーブル

| カラム | 型 | NOT NULL | デフォルト | 説明 |
|---|---|---|---|---|
| id | TEXT | ○ | - | UUID v4（PK） |
| companyName | TEXT | ○ | - | 企業名 |
| loginId | TEXT | - | NULL | マイページログインID |
| myPageUrl | TEXT | - | NULL | マイページURL |
| entryDate | TEXT | - | NULL | エントリー日 (YYYY-MM-DD) |
| nextInterviewDate | TEXT | - | NULL | 次回面接日 (YYYY-MM-DD) |
| position | TEXT | - | NULL | 職種 |
| esContent | TEXT | - | NULL | ES内容 |
| motivation | TEXT | - | NULL | 志望動機 |
| notes | TEXT | - | NULL | メモ |
| transcription | TEXT | - | NULL | 文字起こし結果 |
| status | TEXT | ○ | '未エントリー' | 現在のステータス |
| sortOrder | INTEGER | ○ | 0 | 手動並び順 |
| calendarEventId | TEXT | - | NULL | 端末カレンダーのイベントID |
| createdAt | TEXT | ○ | - | 作成日時 (ISO 8601) |
| updatedAt | TEXT | ○ | - | 更新日時 (ISO 8601) |

### 4.2 selection_events テーブル

| カラム | 型 | NOT NULL | デフォルト | 説明 |
|---|---|---|---|---|
| id | TEXT | ○ | - | UUID v4（PK） |
| companyId | TEXT | ○ | - | 企業ID（FK → companies.id, ON DELETE CASCADE） |
| eventType | TEXT | ○ | - | イベント種別 |
| eventDate | TEXT | - | NULL | 実施日 (YYYY-MM-DD) |
| result | TEXT | ○ | '結果待ち' | 結果（結果待ち / 通過 / 不通過） |
| notes | TEXT | - | NULL | メモ |
| createdAt | TEXT | ○ | - | 作成日時 (ISO 8601) |

### 4.3 custom_statuses テーブル

| カラム | 型 | NOT NULL | デフォルト | 説明 |
|---|---|---|---|---|
| id | INTEGER | ○ | AUTOINCREMENT | PK |
| name | TEXT | ○ (UNIQUE) | - | ステータス名 |
| color | TEXT | ○ | - | 表示色（HEXコード） |
| sortOrder | INTEGER | ○ | 0 | 並び順 |
| createdAt | TEXT | ○ | - | 作成日時 (ISO 8601) |

### 4.4 データベース設定

- **DB名**: `shukatsu.db`
- **外部キー制約**: `PRAGMA foreign_keys = ON`
- **マイグレーション**: ALTER TABLE による列追加（既存列は無視）
- **シングルトン接続**: 初回アクセス時に初期化、以降再利用

---

## 5. 画面別詳細仕様

### 5.1 企業一覧画面 (`app/(tabs)/index.tsx`)

#### 概要
登録済み企業をカード形式でリスト表示する。

#### 表示要素

- **ヘッダー**: 「{N}社を管理中」テキスト + 「並び替え」ボタン
- **企業カード** (CompanyCard):
  - 企業名（1行、超過時は省略）
  - ステータスバッジ
  - 職種（あれば表示）
  - 次回面接日 + 残日数バッジ（あれば表示）
  - エントリー日（あれば表示）
- **空状態**: 🏢アイコン + 「まだ企業が登録されていません」メッセージ + 「右下のボタンから企業を追加してください。」サブテキスト
- **FAB（浮動アクションボタン）**: 右下に「+」ボタン → 企業追加画面へ

#### 操作

| 操作 | アクション |
|---|---|
| カードタップ | 企業詳細画面へ遷移 (`/{id}`) |
| カード長押し（手動順時のみ） | ドラッグ開始（触覚フィードバック付き） |
| ドラッグ&ドロップ | 並び順変更（DB即時保存、失敗時ロールバック） |
| 下に引っ張る | Pull-to-Refresh |
| カード左スワイプ | スワイプ削除（触覚フィードバック + 10秒間「元に戻す」スナックバー表示） |
| 「並び替え」ボタン | 並び替えモーダル表示 |
| FABタップ | 企業追加画面（モーダル） |

#### 残日数バッジ表示ルール

| 条件 | 表示 | スタイル |
|---|---|---|
| 当日 | 「今日」 | 赤背景 |
| 1〜3日後 | 「あと{N}日」 | 赤背景 |
| 4日以上後 | 「あと{N}日」 | インディゴ背景 |
| 過去 | 「{N}日経過」 | グレー背景 |

### 5.2 カレンダー画面 (`app/(tabs)/calendar.tsx`)

#### 概要
月表示のカレンダーで面接予定日を視覚的に確認する。

#### 表示要素

- **月ヘッダー**: 「◀ {YYYY}年{M}月 ▶」（前月・次月ナビゲーション）
- **曜日行**: 日〜土（日曜: 赤、土曜: 青）
- **カレンダーグリッド**: 月の各日を表示
  - 今日: インディゴ薄背景 (`#EEF2FF`)、太字
  - 面接予定あり: 日付の下にインディゴドット
  - 選択中: インディゴ背景、白文字
- **選択日の予定**: 日付タップで該当日の企業一覧を表示（企業名 + StatusBadge、タップで詳細画面へ）
- **今後の面接予定セクション**: 今日以降の面接予定を日付昇順で一覧表示
  - 予定なし時: 「予定されている面接はありません」

#### 操作

| 操作 | アクション |
|---|---|
| ◀ / ▶ タップ | 前月 / 次月に切り替え（選択日リセット） |
| 日付セルタップ | その日の予定を表示（再タップで選択解除） |
| 企業名タップ | 企業詳細画面へ遷移 (`/{id}`) |

#### データソース

- `getAllCompanies('interview')` で全企業を取得し、`nextInterviewDate` をキーにマッピング

### 5.3 企業追加画面 (`app/add.tsx`)

- **表示形式**: モーダル（Stack presentation: 'modal'）
- **ヘッダータイトル**: 「企業を追加」
- **内容**: CompanyForm コンポーネント（初期データなし）
- **送信成功時**: `router.back()` で戻る
- **エラー時**: Alert でエラーメッセージ表示
- **登録上限エラー**: 「企業の登録上限（5社）に達しました」

### 5.4 企業詳細画面 (`app/[id].tsx`)

- **ヘッダータイトル**: 企業名を動的に表示

#### 閲覧モード

表示セクション（上から順）:

1. **ヘッダーカード**
   - 企業名 + ステータスバッジ
   - 「マイページを開く」ボタン（URL設定時のみ表示、`http(s)://` なしの場合は `https://` を自動補完、`Linking.canOpenURL()` でチェック後に開く）
   - ログインID表示エリア
     - 未設定時: 「(未設定)」表示
     - 設定時: ID値 + 「コピー」ボタン + 「編集」ボタン
     - 編集モード: テキスト入力 + 「キャンセル」/「保存」ボタン

2. **基本情報セクション**
   - 職種
   - エントリー日
   - 次回面接日 + 残日数バッジ

3. **選考履歴** (SelectionTimeline)
   - タイムライン形式で選考イベントを表示
   - 「+ 追加」ボタン
   - 各イベントタップで編集モーダル

4. **面接音声** (TranscriptionView)
   - 録音・文字起こし機能（詳細は[8章](#8-音声録音文字起こし)参照）

5. **ES内容セクション**（データがある場合のみ表示）

6. **志望動機セクション**（データがある場合のみ表示）

7. **メモセクション**（データがある場合のみ表示）

8. **アクションボタン**
   - 「編集する」ボタン（インディゴ）
   - 「削除」ボタン（赤）

#### 編集モード

- CompanyForm コンポーネント（initialData に既存データを渡す）
- ヘッダータイトルが「企業を編集」に変更
- 更新成功時: 閲覧モードに戻る

#### 削除確認

- Alert ダイアログ: 「{企業名} を削除しますか？この操作は元に戻せません。」
- 「キャンセル」/「削除」（destructive）
- 削除成功時: 前画面に戻る
- 削除時の処理: 通知キャンセル + カレンダーイベント削除 + DBレコード削除（CASCADE で選考イベントも削除）

### 5.5 企業フォーム (CompanyForm)

#### フィールド一覧

| フィールド | 入力タイプ | 必須 | バリデーション |
|---|---|---|---|
| 企業名 | TextInput | ○ | 空文字不可（trim後） |
| ステータス | モーダルピッカー | ○ | デフォルト「未エントリー」 |
| ログインID | TextInput | - | autoCapitalize: none |
| マイページURL | TextInput (url) | - | http:// or https:// 形式 |
| エントリー日 | DateTimePicker | - | YYYY-MM-DD |
| 次回面接日 | DateTimePicker | - | YYYY-MM-DD |
| 職種 | TextInput | - | - |
| ES内容 | TextInput (multiline) | - | 最小高さ100px |
| 志望動機 | TextInput (multiline) | - | 最小高さ100px |
| メモ | TextInput (multiline) | - | 最小高さ100px |

#### ステータスピッカー

- ボトムシートモーダル（最大高さ70%）
- システムステータス一覧 + カスタムステータス一覧を表示
- 各項目に StatusBadge を表示
- 選択中の項目に「✓」マーク
- 「+ カスタムステータスを追加」ボタン
  - テキスト入力 + 「追加」ボタン
  - カラーは固定値 `#6366F1`

#### 日付ピッカー

- iOS: spinner 表示（モーダル内）
- Android: default 表示
- ロケール: `ja`
- クリアボタン（×）で日付を解除可能

#### 送信制御

- 送信中はボタンを無効化（`isSubmitting` フラグ）
- ボタンテキスト: 送信中「処理中...」、新規「登録する」、編集「更新する」
- 空文字フィールドは `null` に正規化して保存

---

## 6. ステータス管理

### 6.1 システムステータス一覧

以下の順序で定義（配列インデックスが並び順に使用される）:

| # | ステータス名 | 色コード |
|---|---|---|
| 0 | 未エントリー | `#9CA3AF` |
| 1 | ES準備 | `#CBD5E1` |
| 2 | ES提出済み | `#3B82F6` |
| 3 | ES通過 | `#2563EB` |
| 4 | 一次面接予定 | `#FBBF24` |
| 5 | 一次面接済み | `#F59E0B` |
| 6 | 一次通過 | `#D97706` |
| 7 | 二次面接予定 | `#FB923C` |
| 8 | 二次面接済み | `#F97316` |
| 9 | 二次通過 | `#EA580C` |
| 10 | 三次面接予定 | `#F87171` |
| 11 | 三次面接済み | `#EF4444` |
| 12 | 三次通過 | `#DC2626` |
| 13 | 最終面接予定 | `#F472B6` |
| 14 | 最終面接済み | `#EC4899` |
| 15 | 内定 | `#10B981` |
| 16 | 応募停止 | `#6B7280` |
| 17 | 終了 | `#6B7280` |

### 6.2 カスタムステータス

- ユーザーが任意の名前で追加可能
- 色は固定: `#6366F1`
- UNIQUE 制約（同名不可）
- 利用中のカスタムステータスは削除不可（エラー: 「利用中のカスタムステータスは削除できません」）

### 6.3 ステータス自動再計算ロジック

#### トリガー

選考イベントの追加・更新・削除時に、対象企業のステータスがシステムステータスの場合に再計算が実行される。カスタムステータスが設定されている場合は再計算をスキップする。

#### アルゴリズム

1. 企業に紐づく全選考イベントを取得
2. イベントを日付順（昇順）でソート（同日の場合は createdAt 昇順）
3. 各イベントを順に走査:
   - `通過` の場合: イベント種別に応じた次ステータスを適用
   - `不通過` の場合: `応募停止` に設定
   - `結果待ち` の場合: スキップ
4. 該当するイベントがない場合: `未エントリー` にフォールバック

#### 通過時のステータス遷移マップ

| イベント種別 | 通過後のステータス |
|---|---|
| ES提出 | ES通過 |
| 書類選考 | ES通過 |
| 適性検査 | （変更なし） |
| GD | （変更なし） |
| 一次面接 | 一次通過 |
| 二次面接 | 二次通過 |
| 三次面接 | 三次通過 |
| 最終面接 | 内定 |
| リクルーター面談 | （変更なし） |
| カジュアル面談 | （変更なし） |
| その他 | （変更なし） |

#### 不通過時の動作

どのイベント種別でも `応募停止` に遷移する。ただし、不通過イベントの後に通過イベントがある場合は、通過イベントの結果が優先される（時系列順に走査するため）。

---

## 7. 選考イベント管理

### 7.1 イベント種別

```
ES提出 / 書類選考 / 適性検査 / GD / 一次面接 / 二次面接 / 三次面接 / 最終面接 / リクルーター面談 / カジュアル面談 / その他
```

### 7.2 結果

| 結果 | 色コード |
|---|---|
| 結果待ち | `#F59E0B`（黄色） |
| 通過 | `#10B981`（緑） |
| 不通過 | `#EF4444`（赤） |

### 7.3 選考イベントモーダル (AddEventModal)

- **表示**: ボトムシートモーダル（最大高さ85%）
- **フィールド**:
  - イベント種別: 横スクロールのチップ選択（デフォルト: ES提出）
  - 実施日: DateTimePicker（任意）
  - 結果: 3ボタン選択（結果待ち / 通過 / 不通過、デフォルト: 結果待ち）
  - メモ: multiline TextInput（任意）
- **新規追加時**: フォームは初期値にリセット
- **編集時**: 既存データをフォームに展開、「削除」ボタンが表示される
- **削除時**: 確認ダイアログ「この選考イベントを削除しますか？」

### 7.4 タイムライン表示 (SelectionTimeline)

- イベントを `eventDate DESC, createdAt DESC` の順で表示
- 各イベントの左側にドット（結果色）と接続線
- イベントカード内: イベント種別名 + 結果バッジ + 実施日 + メモ（2行まで）
- 空状態: 「選考履歴はまだありません」

---

## 8. 音声録音・文字起こし

### 8.1 状態遷移

```
idle → recording → processing → result
                                  ↓ (やり直す)
                                 idle
```

### 8.2 録音仕様

| 設定項目 | 値 |
|---|---|
| サンプルレート | 16,000 Hz |
| チャンネル数 | 1 (モノラル) |
| ビット深度 | 16 bit |
| オーディオソース | 6 (VOICE_RECOGNITION) |
| 出力形式 | WAV |
| 出力ファイル名 | `shukatsu_interview.wav` |

### 8.3 バックグラウンド録音

#### iOS
- `UIBackgroundModes: ["audio"]` を Info.plist に設定
- `Audio.setAudioModeAsync` で以下を設定:
  - `allowsRecordingIOS: true`
  - `playsInSilentModeIOS: true`
  - `staysActiveInBackground: true`
- 録音停止時に `allowsRecordingIOS: false`, `staysActiveInBackground: false` に戻す
- 録音中は通知バーに「録音中」を表示

#### Android
- `react-native-background-actions` によるフォアグラウンドサービス
- カスタム Expo Config Plugin (`withBackgroundActions.js`) で `foregroundServiceType="microphone"` を AndroidManifest に注入（Android 14+ 対応）
- タスク名: 「面接録音」、タスク説明: 「アプリが音声を記録しています。」
- ディープリンクURI: `shukatsu://`

### 8.4 録音制御

- `busyRef` による多重実行防止（録音開始/停止の連打対策）
- 録音中に `startRecording()` が呼ばれた場合は早期リターン
- 録音停止時に `AudioRecord.init()` は次回録音開始時に再実行

### 8.5 文字起こし (Whisper)

#### モデル情報

| 項目 | 値 |
|---|---|
| モデル | whisper.cpp ggml-small |
| ダウンロードURL | `https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-small.bin` |
| 最小ファイルサイズ | 460 MB |
| 保存先 | `{documentDirectory}/models/ggml-small.bin` |

#### Whisper パラメータ

| パラメータ | 値 |
|---|---|
| language | `ja` |
| translate | false |
| beamSize | 5 |
| temperature | 0.0 |
| prompt | 「就職活動についての日本語音声」 |

#### 処理フロー

1. 録音停止 → WAVファイル保存
2. モデル未初期化の場合:
   - モデルファイルの存在チェック（460MB以上）
   - 不完全な場合は削除して再ダウンロード
   - ダウンロード進捗をUIに表示
   - WhisperContext を初期化（シングルトン）
3. 文字起こし実行
4. 結果をUIに表示 + 自動保存

#### 結果表示

- ヘッダーに「面接音声」タイトル + 「オフライン処理」バッジを表示
- 編集可能な TextInput で表示
- 「保存する」ボタン（変更がある場合のみ有効、保存済み時は「保存済み」表示）
- 「全文をコピー」ボタン
- 「録音を再生」ボタン（録音ファイルがある場合、再生中は「再生中...」表示で無効化）
- 「音声を共有」ボタン（録音ファイルがある場合、`expo-sharing` でWAVファイルを共有）
- 「やり直す」ボタン（確認ダイアログ「文字起こし結果を破棄して、録音をやり直しますか？」、破棄時にDBからも文字起こしを削除してidle状態に戻る）

---

## 9. QRコードスキャン

### 9.1 画面レイアウト

- 上部: カメラプレビュー（高さ280px）
  - 中央にスキャンフレーム（200×200px、紫ボーダー）
  - ヒントテキスト: 「QRコードを枠内に合わせてください」
- 中部: 「画像からQRコードを読み取る」ボタン
- 下部: スキャン履歴リスト

### 9.2 スキャン動作

1. QRコード検出 → `scanned` フラグで二重検出を防止
2. Alert ダイアログで結果表示
   - **URLの場合** (`http://` or `https://` で始まる):
     - 「閉じる」/「コピー」/「URLを開く」ボタン
     - 「URLを開く」→ `Linking.openURL()` で外部ブラウザを起動
   - **URL以外の場合**:
     - 「閉じる」/「コピー」ボタン
3. スキャン履歴に追加（セッション内メモリ保持、永続化なし）

### 9.3 画像からのQRコード読み取り

- 「画像からQRコードを読み取る」ボタンでフォトライブラリから画像を選択
- `expo-camera` の `scanFromURLAsync` で画像内のQRコードを検出
- 検出成功時はカメラスキャンと同様のAlertダイアログを表示
- 検出失敗時: 「画像からQRコードを検出できませんでした」

### 9.4 スキャン履歴

- セッション中のみ保持（アプリ終了で消失）
- 新しい順に表示
- URL形式のレコードはタップで開く
- 長押しでクリップボードにコピー
- 各レコードに「✕」ボタンで個別削除
- 「クリア」ボタンで全削除（確認ダイアログ付き）
- 各レコード: スキャンデータ（2行まで）+ スキャン時刻

### 9.5 カメラ制御

- タブにフォーカスが当たった時にカメラ起動、離れた時に停止
- カメラ権限がない場合: 「権限を許可する」ボタン表示

---

## 10. 通知

### 10.1 面接リマインダー通知

| 項目 | 値 |
|---|---|
| 通知タイミング | ①前日 21:00 ②当日 07:00（それぞれ過去でなければスケジュール） |
| ①前日タイトル | 「明日は面接日です」 |
| ①前日本文 | 「{企業名} の予定を確認して準備しましょう。」 |
| ②当日タイトル | 「本日が面接日です」 |
| ②当日本文 | 「{企業名} の予定を確認してください。」 |
| 識別子 | `interview-eve-{companyId}` / `interview-morning-{companyId}` |
| Androidチャンネル | `interview-reminder`（重要度: HIGH、バイブ: `[0, 250, 250, 250]`、LEDカラー: `#4F46E5`） |

### 10.2 通知ライフサイクル

- **作成**: 企業の作成・更新時に `nextInterviewDate` が設定されている場合
- **更新**: 面接日変更時に既存通知をキャンセルして再作成
- **キャンセル**: 面接日削除時、企業削除時
- **スキップ**: 各通知時刻（前日21:00 / 当日07:00）がそれぞれ現在時刻以前の場合はその通知を作成しない（片方だけスキップされる場合あり）

### 10.3 録音中通知（iOS向け）

| 項目 | 値 |
|---|---|
| タイトル | 「録音中」 |
| 本文 | 「アプリが音声を記録しています。」 |
| 識別子 | `recording-active` |
| sticky | true |
| Androidチャンネル | `recording`（重要度: LOW、バイブなし） |

### 10.4 通知ハンドラー設定

アプリ内で通知を受信した際の動作:

| 設定 | 値 |
|---|---|
| shouldShowAlert | true |
| shouldPlaySound | true |
| shouldSetBadge | false |
| shouldShowBanner | true |
| shouldShowList | true |

### 10.5 権限要求

- アプリ起動時には権限を要求しない
- 面接日設定時、録音開始時など必要なタイミングで初めて要求

---

## 11. カレンダー連携

### 11.1 イベント作成

| 項目 | 値 |
|---|---|
| タイトル | 「{企業名} 面接」 |
| 種別 | 終日イベント |
| 開始日 | 面接日 00:00 |
| 終了日 | 面接日翌日 00:00 |
| メモ | 「就活アプリから作成された予定です。」 |
| アラーム | 60分前 |

### 11.2 カレンダー選択ロジック

#### iOS
- `Calendar.getDefaultCalendarAsync()` でデフォルトカレンダーを取得

#### Android
- 書き込み可能なカレンダーを取得
- 優先順位: isPrimary > Google カレンダー > 先頭のカレンダー

### 11.3 ライフサイクル

- **作成**: 企業作成・更新時に `nextInterviewDate` が設定されている場合
- **更新**: 面接日変更時に既存イベントを更新（失敗時は再作成）
- **削除**: 面接日削除時、企業削除時

---

## 12. 並び替え

### 12.1 並び替えオプション

| オプション | SQLクエリ | 説明 |
|---|---|---|
| 手動順 | `sortOrder ASC, updatedAt DESC` | ドラッグ&ドロップで変更可能 |
| ステータス昇順 | SQL: `sortOrder ASC` → JS側で `getStatusOrderIndex` により再ソート | DEFAULT_STATUS_LIST の配列インデックス昇順 |
| ステータス降順 | SQL: `sortOrder ASC` → JS側で `getStatusOrderIndex` により再ソート | DEFAULT_STATUS_LIST の配列インデックス降順 |
| 面接日順 | `CASE WHEN nextInterviewDate IS NULL THEN 1 ELSE 0 END, nextInterviewDate ASC, updatedAt DESC` | NULL は末尾、同一日は updatedAt DESC |

### 12.2 ドラッグ&ドロップ

- 手動順選択時のみ有効
- 長押し（200ms）で開始、触覚フィードバック（Heavy Impact）
- ドラッグ中のカードは `scale: 1.02`, `opacity: 0.9`
- ドロップ後にトランザクションで全企業の `sortOrder` を一括更新
- 楽観的UI更新（失敗時はロールバック）

---

## 13. バリデーション

### 企業フォーム

| フィールド | ルール | エラーメッセージ |
|---|---|---|
| 企業名 | trim後に空でないこと | 「企業名を入力してください」 |
| マイページURL | null または `new URL()` でパース可能かつ http(s) プロトコルであること。`http(s)://` なしの入力時は `https://` を自動補完 | 「マイページURLの形式が正しくありません」 |
| 企業数上限 | 5社まで | 「企業の登録上限（5社）に達しました」 |

### データ正規化

- 空文字列のフィールドは `null` に変換して保存（`normalizeNullable`）

---

## 14. 権限

| 権限 | 用途 | 要求タイミング |
|---|---|---|
| マイク (RECORD_AUDIO) | 面接音声の録音 | 録音開始時 |
| カメラ (CAMERA) | QRコードスキャン | QRスキャンタブ表示時 |
| フォトライブラリ | 画像からのQRコード読み取り | 画像選択時 |
| カレンダー (READ/WRITE_CALENDAR) | 面接予定の登録 | 面接日設定時 |
| 通知 | 面接日リマインダー | 面接日設定時 |
| フォアグラウンドサービス (Android) | バックグラウンド録音 | 録音開始時 |
| インターネット | Whisper モデルダウンロード | 初回文字起こし時 |

---

## 15. 制限事項

| 項目 | 制限値 |
|---|---|
| 企業登録数上限 | 5社 (`FREE_PLAN_COMPANY_LIMIT`) |
| 選考イベント数 | 制限なし |
| カスタムステータス数 | 制限なし（名前はUNIQUE） |
| 文字起こしモデル | whisper-small (固定) |
| QRスキャン履歴 | セッション内メモリのみ |
| 画面向き | 縦固定 (portrait) |

---

## 16. デザインシステム

### 16.1 カラーパレット

| 用途 | 色コード |
|---|---|
| プライマリ（ブランド） | `#4F46E5` |
| プライマリ背景 | `#EEF2FF` |
| ページ背景 | `#F9FAFB` |
| カード背景 | `#FFFFFF` |
| テキスト（主要） | `#1F2937` |
| テキスト（副次） | `#374151` |
| テキスト（ラベル） | `#6B7280` |
| テキスト（補助） | `#9CA3AF` |
| ボーダー | `#E5E7EB` |
| 入力背景 | `#F3F4F6` |
| 成功・通過 | `#10B981` |
| 警告・待機 | `#F59E0B` |
| エラー・不通過 | `#EF4444` |
| 危険背景 | `#FEE2E2` |
| 危険テキスト | `#DC2626` |

### 16.2 コンポーネントスタイル

#### カード

- 角丸: 16px
- パディング: 16-20px
- シャドウ: `shadowOpacity: 0.05-0.1`, `shadowRadius: 8`
- Elevation: 2-3

#### ボタン

- 角丸: 12px (通常) / 30px (録音ボタン等)
- パディング: 16px (通常) / 14px (コンパクト)
- 無効化時: `opacity: 0.6`

#### モーダル

- ボトムシート形式
- 上部角丸: 20px
- オーバーレイ: `rgba(0, 0, 0, 0.5)`
- アニメーション: slide

#### StatusBadge

- 角丸: 12px
- パディング: 水平10px, 垂直4px
- テキスト: 12px, 白, 太字600

---

## 17. ビルド・リリース

### 17.1 アプリ識別情報

| 項目 | 値 |
|---|---|
| アプリ名 | 就活管理 |
| スラッグ | shukatsu-manager |
| バンドルID (iOS) | com.anonymous.shukatsumanager |
| パッケージ名 (Android) | com.anonymous.shukatsumanager |
| URLスキーム | shukatsu |
| スプラッシュ背景色 | `#4F46E5` |

### 17.2 バージョン管理

以下を同時に更新:
1. `package.json` の `version`
2. `app.json` の `version`
3. `app.json` の `android.versionCode` (+1)
4. `CHANGELOG.md`

セマンティックバージョニング:
- メジャー: 後方互換性のない大きな変更
- マイナー: 新機能追加
- パッチ: バグ修正

### 17.3 ビルドコマンド

```bash
# 開発ビルド
npx expo run:android
npx expo run:ios

# EAS Build
eas build -p android --profile preview

# ローカル Android リリースビルド
npx expo run:android --variant release

# 型チェック
npm run typecheck
```

### 17.4 Expo Config Plugins

| プラグイン | 用途 |
|---|---|
| `./plugins/withBackgroundActions` | Android 14+ フォアグラウンドサービス対応 |
| `expo-router` | ファイルベースルーティング |
| `expo-sqlite` | SQLite ネイティブモジュール |
| `@react-native-community/datetimepicker` | 日付ピッカー |
| `expo-notifications` | 通知 |
| `expo-av` | マイク権限説明文 |
| `expo-file-system` | ファイルシステム |
| `expo-calendar` | カレンダー権限説明文 |
| `expo-camera` | カメラ権限説明文 |

---

## 18. セキュリティ

### 注意事項

- SQLite には応募情報や面接メモを**平文で保存**
- Whisper モデルは初回利用時にインターネット経由でダウンロード
- 文字起こしは完全にオンデバイスで処理（外部サーバーへの送信なし）
- 機微情報を扱う場合は端末の暗号化・ロック設定を推奨
- QRスキャンで取得したURLは自動的に開かれない（ユーザー操作が必要）
